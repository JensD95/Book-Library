import React from 'react';

/*
This is a component that displays a message when a nonexistent page is requested.
There is nothing significantly special about this page, but I figured it was worth implementing.
I have not applied any styling to this page, it exists merely as an error page.
*/

const NotFound = () => {
  return <div>Page Not Found!</div>;
};

export default NotFound;
